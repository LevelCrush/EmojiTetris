# Git LFS tracking for emoji images (disabled - using base64 for GitHub Pages)
# Uncomment below if you want to use Git LFS instead of base64:
# emojis/*.png filter=lfs diff=lfs merge=lfs -text
# emojis/*.gif filter=lfs diff=lfs merge=lfs -text
# emojis/*.jpg filter=lfs diff=lfs merge=lfs -text
# emojis/*.jpeg filter=lfs diff=lfs merge=lfs -text
# emojis/*.webp filter=lfs diff=lfs merge=lfs -text